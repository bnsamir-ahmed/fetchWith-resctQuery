import Landing from "./Landing";
// import { useContext } from "react";
// import useContextProvider from '../../context/context'

const Home = () => {
    // const { h } = useContext(useContextProvider);

  return (
    <>
   
      <Landing />
    </>
  );
};
export default Home;
